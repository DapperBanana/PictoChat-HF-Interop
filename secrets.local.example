# Copy to secrets.local and fill in values — secrets.local is gitignored

# vessel-01 Raspberry Pi
PI_HOST=
PI_HOST_MDNS=vessel-01.local
PI_USER=root-1
PI_PASS=

# Tailscale IP (for remote access through home network)
PI_TAILSCALE_IP=
